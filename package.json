{
  "name": "intelligent-email-assistant",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm --prefix client run build",
    "start": "npm --prefix client run start"
  }
}
